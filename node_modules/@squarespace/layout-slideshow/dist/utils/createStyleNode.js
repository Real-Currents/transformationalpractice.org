'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createStyleNode = undefined;

var _keys = require('babel-runtime/core-js/object/keys');

var _keys2 = _interopRequireDefault(_keys);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Given an object corresponding to a CSS declaration block, create a style node
 * where the innerText is those styles.
 *
 * Example input:
 * '.my-selector': {
 *   position: 'relative',
 *   'z-index': 1
 * };
 *
 * @param  {Object} style
 * @return {HTMLElement}
 */
var createStyleNode = exports.createStyleNode = function createStyleNode(style) {
  var styleNode = document.createElement('style');
  styleNode.innerText = (0, _keys2.default)(style).reduce(function (acc, sel, i) {
    acc += sel + '{';
    (0, _keys2.default)(style[sel]).forEach(function (prop) {
      acc += prop + ':' + style[sel][prop] + ';';
    });
    acc += '}';
    return acc;
  }, '');

  return styleNode;
};